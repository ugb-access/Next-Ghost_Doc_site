const ghostApiUrl = process.env.ghostApiUrl;
const ghostAdminKey = process.env.ghostAdminKey;
const ghostContentKey = process.env.ghostContentKey;

export  {
    ghostApiUrl,
    ghostAdminKey,
    ghostContentKey
}